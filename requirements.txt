# Citation parsing — single source of truth for citation extraction
eyecite==2.6.9
reporters-db==3.2.58

# PDF extraction — pymupdf4llm wraps PyMuPDF and uses its layout GNN to
# produce structured Markdown with body/footnote separation, automatic
# OCR fallback, and seamless page handling. Native PDFs run in ~120 ms.
# PyMuPDF stays as a transitive dep + is still used by scripts/seed.py
# for synthesising opinions that lack a download_url.
PyMuPDF==1.24.10
pymupdf4llm==0.0.17

# API + async runtime
fastapi==0.110.0
uvicorn[standard]==0.27.1
python-multipart==0.0.9

# Database (async SQLAlchemy + Postgres + SQLite drivers, pgvector adapter)
sqlalchemy[asyncio]==2.0.29
asyncpg==0.29.0
aiosqlite==0.19.0
psycopg2-binary==2.9.9
alembic==1.13.1
pgvector==0.2.5

# Embeddings (CPU-only sentence-transformers + torch).
# numpy pinned <2 because torch 2.2.x was compiled against numpy 1.x ABI;
# without this, runtime imports surface a UserWarning ("Failed to
# initialize NumPy") and downstream tensor ops are unstable.
numpy==1.26.4
sentence-transformers==2.7.0
torch==2.2.2

# Settings + validation
pydantic==2.6.4
pydantic-settings==2.2.1
python-dotenv==1.0.1

# Logging
structlog==24.1.0

# Background jobs
redis==5.0.3
arq==0.25.0

# LLM resolver
anthropic==0.39.0

# Async HTTP (replaces requests for CourtListener)
httpx==0.27.0

# Graph analytics
networkx==3.2.1

# Rate limiting
slowapi==0.1.9
